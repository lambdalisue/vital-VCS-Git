let s:V = vital#of('vital')
call s:V.unload() " make sure that the following modules are not cached version
let s:D = s:V.import('VCS.Git.Detector')


Describe VCS.Git.Detector

  Context .new({config})
    It should throw exception if 'cache_dir' is not specified in {config}
    End
  End
End

